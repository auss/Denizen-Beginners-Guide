Musimy iść głębiej: Submapping (JESZCZE NIE NAPISANO)
---------------------------------------------------

**TODO: Opis wyjaśniający koncepcję sub-mappingu. Powinien prezentować sub-mapping zarówno dla flag, jak i definicji oraz prosty sposób ich odczytu, np. „`<[a.b]>`”. Powinien wyjaśniać, jak generowane są obiekty MapTag oraz jak można odczytać klucz główny (root), by otrzymać MapTag, a następnie użyć na nim `.get[]`. Powinien zawierać ogólne uwagi o MapTags. Powinien porównywać użycie sub-mappingu opartego na kropce `.` z bezpośrednim użyciem MapTag do tych samych zadań. Powinien opisywać zarówno poprawę jakości kodu, jak i potencjalne różnice w wydajności. Powinien również prezentować polecenie `definemap`. Powinien pokazywać budowanie definicji lub flagi początkowo z bezpośredniego MapTag, np. `<map[a=b]>`, a następnie modyfikowanie kluczy wewnątrz. Powinien pokazać, że usunięcie korzenia (root) usuwa również węzły wewnątrz. Powinien pokazać, że zastąpienie korzenia skutecznie czyści wszelkie inne klucze wewnątrz, co może być zarówno błędem, jak i użytecznym narzędziem, gdy jest zamierzone.**
